---
import AdminLayout from '@/layouts/AdminLayout.astro';
import { Icon } from 'astro-icon/components';
import { RecipesTable } from '@/components/admin/recipes-table';

const title = 'Recetas - Admin';
---

<AdminLayout title={title}>
  <div class="space-y-6">
    <div class="flex items-center justify-between">
      <div>
        <h1 class="text-3xl font-bold">Recetas</h1>
        <p class="text-muted-foreground mt-2">
          Gestiona las recetas de cocina
        </p>
      </div>
      <div class="flex items-center gap-2">
        <a
          href="/admin/recipes/categories"
          class="inline-flex items-center gap-2 rounded-md border px-4 py-2 text-sm font-medium hover:bg-muted transition-colors"
        >
          <Icon name="lucide:folder" class="h-4 w-4" />
          Categorías
        </a>
        <a
          href="/admin/recipes/new"
          class="inline-flex items-center gap-2 rounded-md bg-primary px-4 py-2 text-sm font-medium text-primary-foreground hover:bg-primary/90 transition-colors"
        >
          <Icon name="lucide:plus" class="h-4 w-4" />
          Nueva receta
        </a>
      </div>
    </div>

    <RecipesTable client:load />
  </div>
</AdminLayout>
